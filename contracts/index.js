/**
 */
let debug  = require('debug')('contracts:server'),
    jayson = require('jayson');

/**
 * Starts contract service with given account and contract.
 * @constructor
 */
class ContractServer {

    /**
     * Returns server instance.
     */
    start(host, port) {
    }


    /**
     * Stop server
     */
    stop() {
    }
}

module.exports = ContractServer;

